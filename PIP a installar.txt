Para el funcionamiento correcto tengo que instlar los siguientes paquetes PIP: 

SRC:.....

pip install flask
pip install flask_login
pip install mysql
pip install mysql-connector-python
pip install bcrypt
pip install Flask-Mail
